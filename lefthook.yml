pre-commit:
  parallel: true
  commands:
    lint-markdown:
      glob: "*.md"
      run: yarn lint:markdown {staged_files}
    lint-markdown-links:
      glob: "*.md"
      run: yarn lint:markdown:links .
